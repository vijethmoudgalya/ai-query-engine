from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='The Retail Query Engine is an interactive natural language interface for querying retail databases. This tool harnesses the power of a large language model (LLM) to convert natural language inputs into SQL queries, making it easier for users without SQL knowledge to access data insights.',
    author='Rushika R',
    license='MIT',
)
